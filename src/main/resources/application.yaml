spring:
  application:
    name: "favorite-manager"
  flyway:
    enabled: true
    locations: "classpath:db/migration"
    baseline-on-migrate: true

springdoc:
  api-docs:
    path: /api-docs.yaml
    enabled: 'false'
